const getDataFromTable = require('./getDataFromTable')
const getDataGlobal = require('./getDataGlobal')

// Este solo es de prueba jaja importo todos los archivos y solo ejecuto el que estoy probando
// pero a la larga nos puede servir para tener todo aquí y correr un único archivo y obtener todos
// los datos

getDataGlobal()